# Agent 项目初步规划

> 用途：切换到新的项目目录后，先让 Codex 阅读本文件，再继续设计和开发。
> 当前状态：Coding Agent 路线一 MVP 已基本实现，正在进行安全加固和测试补全。

> 项目二进度：第四阶段已完成，现已具备持久化向量 RAG、长期记忆、任务看板、人工审批、Cron、基础 Agent Loop、审批门控通知、MCP 动态工具和科研子 Agent；下一阶段实现端到端研究周报。

## 一、两个项目的初步功能分工

### 项目一：CLI Coding Agent

目标是在 VS Code 内置终端、PowerShell 或 CMD 中运行，读取、修改并测试启动目录中的代码项目。

主要功能：

- Agent Loop 与 Tool Calling
- 文件搜索、读取、创建和修改
- Bash/终端命令执行
- 权限检查、Hooks 与错误恢复
- Todo 与上下文压缩
- Git 状态、Diff 和 Worktree（后续增强）
- 基础 Skill（后续按需添加）
- 不实现 MCP；外部业务系统连接统一放在项目二中

不把 Cron、复杂 Agent Teams、团队协议、自动认领任务和长期记忆作为第一版重点。

### 项目二：科研工作流 Agent

面向计算机专业研究生，围绕论文调研、阅读计划、实验管理和研究进展总结提供长期协助。它不是普通问答助手，而是能够连接资料与外部系统、保存研究状态并持续推进任务的工作流 Agent。

重点实现：

- RAG 知识库：检索论文、研究笔记、项目文档和实验记录。
- 长期记忆：保存研究方向、已读论文、实验结论和个人偏好。
- MCP 外部系统连接：按需连接 Zotero、GitHub、文件系统或学术服务。
- 后台任务与 Cron：定期检查新论文、实验结果和任务截止时间。
- 持久化任务看板：管理论文阅读、实验、写作任务及依赖关系。
- 人工审批：发送消息、修改重要资料等操作执行前请求确认。
- 多 Agent 协作：分别负责文献检索、论文总结、实验分析和写作检查。
- 进展通知：生成研究周报并提醒延期任务。

典型输入：

> 帮我调研 Coding Agent 的上下文压缩方法，整理相关论文，建立阅读计划，并每周更新研究进展。

完整演示流程：确定研究课题 → 搜集和筛选论文 → 建立研究计划 → 将论文与笔记加入知识库 → 跟踪阅读和实验任务 → 分析实验结果 → 生成研究周报 → 用户批准后导出或发送。

两个项目的区别：Coding Agent 强调代码理解、修改、测试和安全执行；科研工作流 Agent 强调知识检索、长期研究状态、任务推进、定时调度与外部系统连接。

## 二、Coding Agent 的三种实现路线

### 路线一：单 Agent MVP（已选择）

一个主 Agent 配合文件、命令、权限、Diff 和测试工具。优点是结构清晰，能够展示手写 Agent Harness 的能力，适合作为第一版。

### 路线二：完整 Harness 版

在 MVP 上增加 Todo、Skill、子 Agent、Worktree 等能力。Coding Agent 不实现 MCP；MCP 集中放在项目二中，用于连接外部业务系统。功能更强，但第一版容易臃肿，安排在后续迭代。

### 路线三：LangGraph 工作流版

使用 LangGraph 把“分析、修改、测试、修复”表示成状态图。流程更加明确，但不适合作为手写底层机制的第一版。可以在原生版完成后重构实现，用于对比两种方案。

## 三、Coding Agent 第一版核心功能

- 将启动目录设为工作区，并限制访问工作区外部路径。
- 提供与 s20 一致的 `glob`、`read_file` 等代码查找和读取工具。
- 使用 Patch/Diff 方式修改文件，避免不透明地覆盖整个文件。
- 执行终端命令，并支持超时、输出截断和错误返回。
- 修改前或敏感操作前展示 Diff，并请求用户确认。
- 自动识别并运行项目测试、构建或静态检查命令。
- 测试失败时把错误信息交回 LLM，允许继续分析和修复。
- 设置最大 Agent 循环次数、Token/成本及工具调用限制。
- 保存当前会话上下文，并在过长时进行压缩。
- 记录工具调用、修改、测试与错误日志。
- 展示 `git status` 和最终修改摘要。
- 支持通过 `Ctrl+C` 安全中断当前任务。
- 最终报告修改内容、验证结果和遗留问题。

第一版明确不包含：多 Agent、Cron、持久化任务看板和长期记忆。Coding Agent 的后续路线同样不实现 MCP。

## 四、下一阶段

1. Coding Agent：完成命令安全、日志脱敏、工具协议配对和异常恢复加固。
2. Research Workflow Agent：实现端到端研究周报生成与评估。
3. 为 Zotero、GitHub 选择实际 MCP Server，并在本地私有配置中接入。
4. 增加真实模型的集成测试和完整演示数据。

# s01-s20 章节总结

## 总体关系

这 20 章围绕同一个 Agent Harness 逐步增加能力。Harness 是连接 LLM 与工具、文件系统、权限、记忆和其他 Agent 的运行框架。多数章节延续前一章的思路继续追加机制；但为了让教学代码保持精简，部分后期章节会省略与本章无关的旧实现，因此不能简单理解为每个 `code.py` 都完整包含之前的全部代码。s20 最后把所有机制重新整合起来。

## s01：Agent Loop

建立最基础的 Agent 循环：LLM 请求工具时，程序执行工具并把结果发回；LLM 不再调用工具时结束。它是整个项目的起点，不依赖前面的章节。

## s02：Tool Use

在 s01 的循环上增加 read、write、edit、glob 等工具，并使用 `TOOL_HANDLERS` 统一分发。它说明新增工具只需注册定义和处理函数，不必修改核心循环。

## s03：Permission

在 s02 的工具执行前加入权限检查，包括硬性禁止、风险规则和用户确认。核心循环基本不变，只是在真正执行工具前增加一道安全门。

## s04：Hooks

在 s03 基础上加入 Hook，把权限、日志、输出检查和结束处理注册到不同事件中。这样扩展行为不需要不断修改 Agent 主循环。

## s05：TodoWrite

在 s04 基础上注册 `todo_write` 工具，让 LLM 创建并更新带状态的任务清单。Todo 主要帮助 LLM 和用户看清进度，并通过提醒机制减少长任务中途偏离。

## s06：Subagent

在 s05 基础上增加 `task` 工具，可创建拥有全新 `messages[]` 的子 Agent。子 Agent 独立完成小任务，只把最终摘要返回主 Agent，从而减少主上下文污染。

## s07：Skill Loading

在 s06 基础上增加按需技能加载。启动时只把技能目录放入系统提示，LLM 真正需要某项知识时再调用 `load_skill` 读取完整内容，避免 system prompt 过长。

## s08：Context Compact

在 s07 的整体骨架上加入上下文压缩。它依次裁剪旧消息、压缩旧工具结果、限制大输出，必要时再调用 LLM 生成摘要，让长时间运行的 Agent 不容易超过上下文窗口。

## s09：Memory

在 s08 的压缩能力上增加持久记忆，把用户偏好、反馈、项目背景等保存到 `.memory/`。压缩可能丢失细节，而 Memory 负责把重要信息跨轮次、跨会话保留下来；本章为突出记忆，精简了部分旧工具。

## s10：System Prompt

以 s08-s09 的能力为背景，把写死的 system prompt 改成运行时分段组装。系统根据实际工具、工作区和记忆状态选择提示内容，并缓存不变的组装结果；本章重点是 prompt 管理，并非完整累加所有旧实现。

## s11：Error Recovery

在 s10 的循环和 prompt 组装上加入错误恢复。它处理输出被截断、上下文超限和 429/529 临时错误，并通过扩大输出限制、压缩上下文、退避重试或备用模型继续运行。

## s12：Task System

在基础 Agent 循环上加入可持久化的任务系统，用 `.tasks/*.json` 保存任务、负责人、状态和依赖关系。它比 s05 的 Todo 更适合跨会话协作；任务系统与 s11 的错误恢复相互独立，因此本章省略了完整错误恢复实现。

## s13：Background Tasks

在 s12 的简化任务系统上加入后台执行。慢命令可以在线程中运行，主 Agent 不必一直等待；完成结果会在后续轮次以通知形式注入对话。

## s14：Cron Scheduler

在 s13 基础上加入定时调度器。独立线程按 cron 表达式触发任务并放入队列，Agent 空闲时自动消费，因此没有新的用户输入也能按计划工作。

## s15：Agent Teams

在 s14 的任务、后台和调度能力脉络上加入多 Agent 团队。Lead 可以创建长期队友，队友通过文件收件箱异步通信；它不同于 s06 用完即销毁、只返回一次摘要的子 Agent。

## s16：Team Protocols

在 s15 的团队通信上加入结构化请求—响应协议。关机请求和计划审批通过 `request_id` 关联，并记录 pending、approved、rejected 等状态，使队友协调不再只依赖松散文本。

## s17：Autonomous Agents

在 s16 基础上让队友具备自治能力。队友空闲时会轮询消息和任务看板，自动认领依赖已满足的任务，完成后继续寻找工作，不再要求 Lead 逐个分配。

## s18：Worktree Isolation

在 s17 的自治团队上加入 Git worktree 隔离。每项任务可以绑定独立目录和分支，让多个 Agent 并行修改代码时互不覆盖，并记录 worktree 的创建、保留和清理过程。

## s19：MCP Tools

在 s18 基础上加入 MCP 外部工具接入。Agent 可以通过标准协议发现和调用外部服务的工具，并用 `mcp__server__tool` 命名空间避免冲突，不再要求所有工具都写死在 Harness 中。

## s20：Comprehensive Agent

s20 不是增加一种全新的机制，而是把前 19 章拆开讲解的能力整合到一个可运行 Harness 中。工具、权限、Hooks、规划、技能、压缩、记忆、错误恢复、任务、后台调度、团队、worktree 和 MCP 最终仍围绕同一个 Agent Loop 协作。


| 章节 | 学习难度 | 时间投入 | 预计时间 |
|---|---:|---:|---:|
| s01 Agent Loop | ★★☆☆☆ | ★☆☆☆☆ | 1–2 小时 |
| s02 Tool Use | ★★☆☆☆ | ★★☆☆☆ | 1–2 小时 |
| s03 Permission | ★★☆☆☆ | ★★☆☆☆ | 1–2 小时 |
| s04 Hooks | ★★★☆☆ | ★★☆☆☆ | 2–3 小时 |
| s05 TodoWrite | ★★☆☆☆ | ★★☆☆☆ | 1–2 小时 |
| s06 Subagent | ★★★☆☆ | ★★★☆☆ | 2–4 小时 |
| s07 Skill Loading | ★★★☆☆ | ★★☆☆☆ | 2–3 小时 |
| s08 Context Compact | ★★★★☆ | ★★★★☆ | 4–6 小时 |
| s09 Memory | ★★★★☆ | ★★★★☆ | 4–6 小时 |
| s10 System Prompt | ★★★☆☆ | ★★★☆☆ | 2–4 小时 |
| s11 Error Recovery | ★★★★☆ | ★★★★☆ | 4–6 小时 |
| s12 Task System | ★★★☆☆ | ★★★☆☆ | 3–5 小时 |
| s13 Background Tasks | ★★★★☆ | ★★★★☆ | 4–6 小时 |
| s14 Cron Scheduler | ★★★★☆ | ★★★★☆ | 4–6 小时 |
| s15 Agent Teams | ★★★★★ | ★★★★★ | 6–10 小时 |
| s16 Team Protocols | ★★★★★ | ★★★★★ | 6–10 小时 |
| s17 Autonomous Agents | ★★★★★ | ★★★★★ | 6–10 小时 |
| s18 Worktree Isolation | ★★★★☆ | ★★★★☆ | 4–7 小时 |
| s19 MCP Tools | ★★★★☆ | ★★★★☆ | 4–7 小时 |
| s20 Comprehensive | ★★★★★ | ★★★★★ | 8–12 小时 |